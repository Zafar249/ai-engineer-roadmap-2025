# LinkedLists stores Nodes in 2 parts : Data + Address(pointers)
# Nodes are in non-consecutive memory locations
# Elements are linked using pointers
# LinkedList is a dynamic data structure (allocates needed memory when running)